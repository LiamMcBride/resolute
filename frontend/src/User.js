export class User {
    constructor(username, firstName, lastName, age){
        this.username = username
        this.firstName = firstName
        this.lastName = lastName
        this.age = age
    }
}